const TileTypes = require('./TileTypes');

/**
 * This class unifies tile properties
 */
class Tile {
    constructor(){
        this.type = TileTypes.Default;
    }

    /*
        Walkable?
        Constructuble on?
        etc...

        Maybe Elevation? itf
    */
}

module.exports = Tile;